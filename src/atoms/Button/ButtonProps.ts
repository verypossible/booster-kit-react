export interface ButtonProps extends Theme {
  background?: string,
  children: any,
  className?: string,
  color?: string,
  disabled?: boolean,
  fill?: boolean,
  icon?: string,
  iconColor?: string,
  id?: string,
  invert?: boolean,
  onClick?: any,
  status?: ThemeStatusSelector,
  text?: string,
  type?: string
}
