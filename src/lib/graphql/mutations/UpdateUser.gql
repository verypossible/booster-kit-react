mutation UpdateUser($input: UpdateUserInput!) {
  updateUser(input: $input) {
    changedUser {
      avatar
      email
      expiresAt
      githubUsername
      googleUsername
      id
      name
      username
    }
  }
}
