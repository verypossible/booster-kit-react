import bgColor, { BgColor } from './bgColor'
import color, { Color } from './color'
import common, { Common } from './common'
import flex, { Flex } from './flex'
import font, { Font } from './font'
import grid, { Grid } from './grid'
import spacing, { Spacing } from './spacing'

/** Types */
export {
  BgColor,
  Color,
  Common,
  Flex,
  Font,
  Grid,
  Spacing
}

/** Helpers */
export {
  bgColor,
  color,
  common,
  flex,
  font,
  grid,
  spacing
}
