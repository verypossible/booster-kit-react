/* @flow */
import WithSubRoutes from './WithSubRoutes'
import { Redirect, Miss, Match, NavigationPrompt } from 'react-router'

const match = {
  Match,
  Miss,
  NavigationPrompt,
  WithSubRoutes,
  Redirect
}

export default match
